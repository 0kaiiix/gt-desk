services:
  # Render Static Site Configuration for Vite + React SPA
  - type: web
    name: office-pedestal-system
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
